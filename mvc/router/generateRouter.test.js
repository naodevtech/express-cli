const CreateRouterService = require('./generateRouter');

test("Should create a new log directory if one doesn't already exist", async () => {
  setTimeout(() => {
    expect(createControllerService()).toBe(String);
  }, 1000);
});
